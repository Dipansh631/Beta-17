# Server Configuration
PORT=3000

# Gemini API Key (Required for ID extraction and face verification)
# Get your API key from: https://makersuite.google.com/app/apikey
# Gemini Pro Vision is used for both OCR and data structuring
GEMINI_API_KEY=your_gemini_api_key_here

# MongoDB Configuration (Required for file storage)
# Local MongoDB: mongodb://localhost:27017
# MongoDB Atlas: mongodb+srv://username:password@cluster.mongodb.net/
MONGODB_URI=mongodb://localhost:27017
MONGODB_DB_NAME=community_donation_tracker

# Firebase Admin SDK (Optional - for Firestore features)
# Download serviceAccountKey.json from Firebase Console
# Place it in backend/ folder
GOOGLE_APPLICATION_CREDENTIALS=./serviceAccountKey.json

# CORS Origins (comma separated)
# Add your frontend URLs here
CORS_ORIGINS=http://localhost:8080,http://localhost:3000,http://localhost:8081

# Node Environment
NODE_ENV=development

